%h1 All tasks

- @tasks.each do |task|
  - if task.user_id != current_user.id
    = task.name
    |
    = task.location
    |
    = task.description
    |
    = task.user_id
    |
    = task.tokens
    |
    = task.category
    |
    = link_to 'Claim Task', assign_path(:task_id => task.id)

  - else
    = task.name
    |
    = task.location
    |
    = task.description
    |
    = task.user_id
    |
    = task.tokens
    |
    = task.category
    |
    = link_to 'Edit Task', edit_task_path(task)
  %br
